# CGV 상영시간표 앱 제작 지시문

아래 내용을 그대로 복사해서 제미나이에 붙여넣으세요.

---

## 만들어야 할 것

Node.js + Express 기반의 로컬 웹앱을 만들어줘. CGV 상영시간표를 조회해서 화면에
표시하고 Excel로 내려받을 수 있으며, 영화명을 클릭하면 팝업으로 영화 상세정보와
평점을 보여주는 앱이야.

**중요: 아래 "반드시 지켜야 할 함정" 절의 내용은 실제로 부딪혀서 확인한 사실이야.
추측으로 다르게 구현하지 말고 그대로 따라줘. 특히 네이버 관련 부분은 문서에 없는
내용이라 직접 알아내기 매우 어려워.**

---

## 1. 폴더 구조

```
cgv-schedule-app/
├── package.json
├── .env                ← API 키 (커밋 금지)
├── .gitignore          ← node_modules, .env
├── server.js           ← Express 서버 (CGV·영화정보 프록시 + 정적 서빙)
└── public/
    ├── index.html      ← 화면 전체 (HTML + CSS + JS 한 파일)
    └── theaters.js     ← 전국 CGV 지역/극장 코드 (서버·브라우저 공용)
```

의존성은 `express`와 `undici` 두 개만 사용하고, CommonJS(`require`)로 작성해줘.
Node 18 이상 기준이야.

---

## 2. 극장 데이터 (`public/theaters.js`)

전국 CGV 극장을 지역별로 담은 파일이야. 아래 형태로 만들어줘.

```js
const CGV_REGIONS = [
  { region: "서울", theaters: [ { name: "강남", code: "0056" }, ... ] },
  { region: "경기", theaters: [ { name: "오리", code: "0004" }, ... ] },
  ...
];

const CGV_DEFAULT_REGION  = "경기";
const CGV_DEFAULT_SITE_NO = "0004";   // CGV 오리

// 브라우저에서는 <script src="theaters.js">로 전역 사용,
// 서버에서는 require("./public/theaters.js")로 사용
if (typeof module !== "undefined" && module.exports) {
  module.exports = { CGV_REGIONS, CGV_DEFAULT_REGION, CGV_DEFAULT_SITE_NO };
}
```

**이 한 파일을 브라우저와 서버가 공유하는 게 핵심이야.** 서버의 극장코드
화이트리스트를 이 데이터에서 자동 생성해서, 극장을 추가할 때 한 곳만 고치면 되게 해줘.

```js
const ALLOWED_SITE_NOS = new Set(
  CGV_REGIONS.flatMap(r => r.theaters.map(t => t.code))
);
```

지역은 9개(서울, 경기, 인천, 강원, 대전/충청, 대구, 부산/울산, 경상,
광주/전라/제주)이고 극장은 총 178개야. 극장 목록은 내가 따로 줄 테니
일단 구조만 만들고 몇 개 샘플로 채워둬. 코드는 4자리 문자열이고
씨네드쉐프 지점은 `P001`, `P004`, `P013`처럼 P로 시작하니 문자열로 다뤄야 해.

---

## 3. 화면 (`public/index.html`)

한 파일 안에 HTML·CSS·JS를 모두 넣어줘. 외부 라이브러리는 Excel 생성용
SheetJS만 CDN으로 불러와.

```html
<script src="https://cdn.jsdelivr.net/npm/xlsx/dist/xlsx.full.min.js"></script>
<script src="theaters.js"></script>
```

**조회 영역**
- 날짜 입력(`<input type="date">`) — 로드 시 오늘 날짜 자동 설정
- **지역 선택** → 선택한 지역의 극장만 채워지는 **극장 선택** (2단 연동)
- 초기값은 **경기 / CGV 오리**
- 극장 표시명은 `CGV 오리` 형식 (다운로드 파일명에 쓰임)
- "상영시간표 조회" 버튼

**결과 영역**
- 표 컬럼: `영화명`, `날짜`, `상영관`, `상영시간 (시작~끝)`
- 정렬: 영화명 → 날짜 → 상영관 → 시작시간 (상영관은 숫자 인식 정렬)
- "Excel 다운로드" 버튼 → 파일명 `{극장명}_{YYYYMMDD}_상영시간표.xlsx`,
  컬럼 폭 지정과 자동 필터 포함

**디자인**: CGV 레드(`#e5101d`) 포인트, 흰 카드에 옅은 그림자, 회색 배경.

---

## 4. CGV 조회 API

`GET /api/schedule?siteNo=0004&scnYmd=20260820`

서버가 대신 CGV를 호출해서 브라우저 CORS를 우회하는 구조야.

```
https://cgv.co.kr/api/v1/booking/searchMovScnInfo
  ?coCd=A420&siteNo={siteNo}&scnYmd={scnYmd}&rtctlScopCd=08
```

- 호출 시 브라우저 `User-Agent`와 `Referer: https://cgv.co.kr/` 헤더를 꼭 붙여줘
- `siteNo`는 화이트리스트 검증, `scnYmd`는 `/^\d{8}$/` 검증
- 타임아웃 10초 (`AbortController`)

**응답 필드 매핑**

| 화면 컬럼 | CGV 필드 |
|---|---|
| 영화명 | `expoProdNm` → `prodNm` → `movNm` 순으로 사용 |
| 날짜 | `scnYmd` (`20260820` → `2026-08-20`) |
| 상영관 | `scnsNm` |
| 시작/종료 | `scnsrtTm` / `scnendTm` |

시간은 `0940` → `09:40` 형태로 변환하는데, **24시를 넘는 심야 상영은
`2435`, `2605`처럼 들어와.** 24 이상이면 `익일 00:35`, `익일 02:05`로 표시해줘.

**사내망 대응**: 인증서 검증 오류(`UNABLE_TO_VERIFY_LEAF_SIGNATURE`)가 나는
환경이 있어서, `ALLOW_INSECURE_TLS=true` 환경변수일 때만 undici `Agent`로
검증을 생략하는 선택적 우회를 넣어줘. 기본값은 정상 검증이야.

---

## 5. 영화 상세정보 API

`GET /api/movie?title=파묘`

세 곳에서 정보를 모아 하나로 합쳐서 응답해. **한 곳이 실패해도 나머지는
정상 응답해야 해** (각각 `available: true/false` + `reason`).

| 정보 | 출처 |
|---|---|
| 포스터, 감독, 출연진, 줄거리, 개봉일, 러닝타임, 장르 | TMDB |
| 관객 평점, 성별 평점, 관객 한줄평 | 네이버 |
| **전문가(평론가) 평점 + 코멘트** | 네이버 |
| IMDb 평점 | OMDb |
| 해외 전문가 평점(Metacritic, Rotten Tomatoes) | OMDb |

**호출 순서**: TMDB와 네이버를 병렬로 호출하고, TMDB가 준 `imdb_id`로 OMDb를 호출해.

**API 키**는 `.env`에서 읽되 별도 패키지 없이 직접 파싱해줘. 이미 환경변수가
설정돼 있으면 그 값을 우선해. 키가 없어도 앱이 죽지 않고 해당 항목만 빠져야 해.

```
TMDB_API_KEY=...
OMDB_API_KEY=...
```

**캐시**: OMDb 무료 키가 하루 1,000건 제한이라 결과를 캐시해야 해. 단
**정상 조회는 6시간, 일부라도 실패한 결과는 3분만** 캐시해줘. 이유는 아래 함정 6번.

---

## 6. 반드시 지켜야 할 함정 (실제로 확인한 사실)

### ① 네이버 영화 정보는 공식 API가 없다

네이버 영화 서비스는 2023년 3월 종료됐고 검색 API의 영화 항목도 2023년 8월
종료됐어. 그래서 **통합검색 HTML 파싱 + 검색 화면이 내부적으로 쓰는 패널 API**
두 가지를 조합해야 해.

### ② 검색어에 반드시 "평점"을 붙여야 한다

이게 가장 중요해. 검색어에 "평점"이 없으면 영화 패널이 렌더링되지 않아서
**영화 코드(mcode)를 얻을 수 없고, 전문가 평점과 한줄평이 통째로 빠져.**

```
"영화 파묘"        → mcode 없음 ❌
"영화 파묘 평점"   → mcode 236086 ✅
```

검색 URL: `https://search.naver.com/search.naver?query=` + `영화 {제목} 평점`
헤더에 브라우저 `User-Agent`와 `Accept-Language: ko-KR`을 붙여줘.

mcode 추출:
```js
html.match(/fileKey=movieKB\w*PointAPI&(?:amp;)?u1=(\d+)/) || html.match(/mcode=(\d+)/)
```

### ③ 전문가(평론가) 평점은 별도 API로 가져온다

검색 HTML에는 "평론가" 탭 **이름표만** 있고 내용이 없어. 그래서 HTML만 훑으면
"전문가 평점이 없다"고 잘못 판단하게 돼. 실제로는 아래 API에 다 있어.

```
평론가 평점·코멘트:
https://ts-proxy.naver.com/content/nqapirender.nhn
  ?where=nexearch&pkid=68&fileKey=movieKBExpertPointAPI&u1={mcode}

관객 한줄평:
https://ts-proxy.naver.com/dcontent/nqapirender.nhn
  ?where=nexearch&pkid=68&fileKey=movieKBPointAPI&u1={mcode}&u5={true 또는 빈값}
```

응답은 `{ "html": "..." }` 형태라 `html` 값을 꺼내 태그를 걷어내고 파싱해.
`Referer: https://search.naver.com/` 헤더를 붙여줘.

**평론가 파싱** — 평문 형태는 `이름 박평식 작성일 2024.02.22 별점(10점 만점 중) 6.0 난폭, 변덕, 애국의 삽질`

```js
/이름\s+(\S+(?:\s\S+)?)\s+작성일\s+([\d.]+)\s+별점\(10점\s*만점\s*중\)\s+([\d.]+)\s+(.+?)(?=\s+이름\s+\S+\s+작성일|\s*$)/g
```

**한줄평 파싱**
```js
/별점\(10점\s*만점\s*중\)\s*(\d{1,2})\s+(.+?)\s*(?:펼쳐보기\s*)?작성자\s+(\S+)\s+작성일\s+([\d.]+)[^\d]*[\d:]*\s*신고여부\s*신고\s*([\d,]+)/g
```
본문 앞에 `관람객`이라는 뱃지 문구가 붙는 경우가 있으니 제거해줘.

평론가 평점은 개별 점수의 평균을 내서 대표값으로 보여줘.

### ④ 관객 평점 라벨이 영화마다 다르다

관객이 많으면 "실관람객 평점", 적으면 "네티즌 평점"으로 표기돼.
**둘 다 인식해야 해.** 한쪽만 처리하면 소규모 영화는 평점이 통째로 누락돼.

```js
/(실관람객|네티즌)\s*평점[^0-9]{0,40}(\d{1,2}\.\d{1,2})\s*(?:10)?\s*([\d,]+)\s*명\s*참여/
```

| 영화 | 라벨 |
|---|---|
| 인터스텔라 (17,074명) | 실관람객 평점 9.13 |
| 지난 여름 (7명) | 네티즌 평점 6.43 |

한줄평 API의 `u5` 값도 이 라벨에 맞춰 `실관람객`이면 `true`, `네티즌`이면 빈 값으로 보내.
성별 평점(`남자 9.19 여자 9.05`)도 함께 파싱해줘.

### ⑤ mcode가 없으면 평점을 버려야 한다

패널이 안 뜬 페이지에서도 정규식이 **다른 영화의 평점을 우연히 잡아채는** 경우가 있어.
실제로 "스파이더맨-브랜드 뉴 데이" 검색 시 8.67이 잡혔는데 실제 값은 9.01이었어.
**mcode를 못 얻으면 평점을 신뢰하지 말고 "정보 없음"으로 처리해줘.**

### ⑥ CGV 제목과 네이버 제목이 다르면 재시도해야 한다

CGV는 콜론 대신 하이픈을 쓰는 경우가 있어서 네이버가 영화를 못 찾아.

| CGV | 네이버 |
|---|---|
| 스파이더맨**-**브랜드 뉴 데이 | 스파이더맨**:** 브랜드 뉴 데이 |

그래서 **네이버가 mcode를 못 찾았고 TMDB는 영화를 찾았다면, TMDB의 정식 제목으로
네이버를 한 번 더 조회**해줘. 이걸 안 하면 전문가 평점과 한줄평이 다 빠져.

그리고 이런 일시적 실패가 6시간 캐시되면 실제로는 정보가 있는 영화가 계속
"정보 없음"으로 보여. 그래서 5번 절의 캐시 분리(정상 6시간 / 실패 3분)가 필요해.

### ⑦ 영화 제목에서 괄호를 모두 제거해야 한다

CGV 영화명에는 상영 포맷과 부가정보가 붙어 있어. **키워드 목록으로 걸러내지 말고
괄호는 내용과 무관하게 통째로 제거해줘.** 키워드 방식은 `(SCREENX 2D)` 같은 걸
못 걸러서 `스파이더맨-브랜드 뉴 데이( 2D)`처럼 깨진 검색어를 만들어.

```
스파이더맨-브랜드 뉴 데이(SCREENX 2D)   → 스파이더맨-브랜드 뉴 데이
라스트 키스(가브리엘레 무치노 감독전)     → 라스트 키스
[IMAX](자막)듄: 파트2                  → 듄: 파트2
```

대괄호 `[]`, 소괄호 `()`를 모두 제거하고, 괄호 밖에 붙은
`IMAX`, `SCREENX`, `4DX`, `SPHEREX`, `DOLBY CINEMA` 등 포맷 표기도 지워줘.
제거 후 빈 문자열이 되면 원본을 그대로 써.

괄호가 제목의 일부인 영화도 있을 수 있으니, **괄호를 제거한 검색이 실패하면
괄호를 살린 제목으로 한 번 더 검색**하는 예비 경로도 넣어줘.

### ⑧ TMDB는 인기도순으로 고르면 안 된다

인기도만으로 정렬하면 최신 속편이 뽑혀버려.

```
"아바타"  → 아바타: 불과 재  ❌  (정답: 2009년 아바타)
"위키드"  → 위키드: 포 굿    ❌  (정답: 2024년 위키드)
```

**제목 완전 일치를 최우선**으로 하고, 그 다음 검색어로 시작하는 것,
마지막으로 인기도 순으로 정렬해줘. 비교할 때는 공백·콜론·하이픈·마침표를
제거하고 소문자로 바꿔서 비교해.

### ⑨ 사람 이름은 한글 또는 영문으로 보여줘야 한다

TMDB 한국어 데이터에 한자·가나 이름이 그대로 남아있는 경우가 있어.

| 영화 | ko-KR | en-US |
|---|---|---|
| 화양연화 | `萧炳林` | `Siu Ping-Lam` |
| 패왕별희 | `吕齐`, `葛优` | `Lü Qi`, `Ge You` |

이름이 한글도 영문도 아니면 **영어 크레딧(`/movie/{id}/credits?language=en-US`)의
로마자 표기로 대체**해줘. 배우명과 배역명 모두 적용하고, 영어 쪽에도 쓸 만한 값이
없으면 원래 값을 유지해. 이 추가 호출은 **실제로 그런 이름이 있을 때만** 하도록 해서
평소 호출 수가 늘지 않게 해줘.

판정은 이렇게:
```js
const stripped = String(value || "").replace(/[\s0-9\p{P}\p{S}]/gu, "");
if (!stripped) return true;                      // 빈 값은 그대로 둠
return /^[\p{Script=Hangul}\p{Script=Latin}]+$/u.test(stripped);
```

### ⑩ IMDb는 직접 스크래핑할 수 없다

`imdb.com`을 직접 요청하면 **HTTP 202에 빈 본문**이 와 (봇 차단).
반드시 OMDb API를 통해서 가져와야 해. OMDb 응답 하나에 IMDb·Metacritic·
Rotten Tomatoes가 다 들어있어서 이거 하나면 충분해.

```json
"Ratings": [
  {"Source": "Internet Movie Database", "Value": "8.7/10"},
  {"Source": "Rotten Tomatoes",         "Value": "73%"},
  {"Source": "Metacritic",              "Value": "74/100"}
],
"imdbRating": "8.7", "Metascore": "74"
```

`N/A` 문자열이 오는 필드가 많으니 `null`로 정규화해줘.
OMDb는 `imdb_id`로 조회(`?i=tt0816692`)해야 정확히 매칭돼.

---

## 7. 영화 정보 팝업 (UI)

결과 표의 **영화명을 클릭**하면 모달이 떠야 해.

**구성**
1. 상단: 포스터(왼쪽) + 제목·원제·장르 태그·감독·개봉일·러닝타임·태그라인(오른쪽)
2. 평점 카드 가로 나열 — 각각 데이터가 있을 때만 표시
   - 관객 평점(네이버) — 라벨은 실제 표기(`실관람객`/`네티즌`)를 그대로 사용
   - **전문가 평점(네이버)** — 평론가 평균, 인원수 표기
   - IMDb
   - 전문가(Metacritic)
   - 전문가(Rotten Tomatoes)
   - TMDB
3. 줄거리
4. 출연진 — 프로필 사진 + 이름 + 배역 (사진 없으면 "사진 없음" 자리표시)
5. **전문가 평(네이버 평론가)** — 별점 뱃지 + 코멘트 + 평론가 이름·작성일
6. 관객 한줄평 — 별점 뱃지 + 본문 + 작성자·작성일·공감수
7. 하단: IMDb·네이버 원문 링크, 출처 표기, 못 가져온 항목 안내

**동작**
- 로딩 중 표시
- 닫기 3가지: X 버튼 / ESC 키 / 배경 클릭 (모달 안쪽 클릭은 닫히면 안 됨)
- 열릴 때 `body` 스크롤 잠금, 닫을 때 복원
- 평점 카드 색 구분: 네이버 관객 초록, 네이버 전문가 보라,
  IMDb 노랑, Metacritic 파랑, RT 빨강

**보안**: 영화명이 그대로 `onclick` 속성과 HTML에 들어가니 **반드시 escape** 해줘.
`&`, `<`, `>`, `"`, `'` 모두 처리해야 XSS가 안 생겨.

---

## 8. 마무리

- README.md에 실행법, API 키 발급 방법(TMDB/OMDb 링크), 위 함정들의 요약을 적어줘
- `.env`는 `.gitignore`에 넣어줘
- 서버 시작 시 콘솔에 포트와 키 설정 여부를 출력해줘

작업을 마치면 아래 영화들로 검증해줘. 각각 다른 함정을 건드리는 사례야.

| 검증용 영화 | 확인할 것 |
|---|---|
| 파묘 | 실관람객 라벨, 전문가 평점 7명 |
| 지난 여름 | **네티즌** 라벨(관객 7명), 전문가 8명 |
| 스파이더맨-브랜드 뉴 데이(SCREENX 2D) | 괄호 제거 + **하이픈 재시도** |
| 라스트 키스(가브리엘레 무치노 감독전) | 괄호 제거 |
| 화양연화 / 패왕별희 | 한자 이름 → 로마자 변환 |
| 아바타 / 위키드 | 속편이 아닌 **원작**이 나오는지 |
| 인터스텔라 | 전 항목 정상 |
